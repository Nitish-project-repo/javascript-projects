/* 
    DOM : Document Object Model
    It is the document-object's model structure represented in a tree structure.
        > Window node is the root.
        > Child node -> document node -> html node -> head node & body node
        > head node and body node have there own child node

    Node represents the parent and child objects and its properties in the window object 
        which represents the stucture of the document of the website.
*/